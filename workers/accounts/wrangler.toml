# Account service for reames-agent.io — email/password auth, sessions, profiles.
# First deploy:
#   wrangler d1 create reames-agent-accounts        # paste database_id below
#   wrangler d1 migrations apply reames-agent-accounts --remote
#   wrangler secret put SESSION_PEPPER          # any long random string
#   wrangler secret put RESEND_API_KEY          # only if EMAIL_PROVIDER=resend
#   wrangler deploy
name = "reames-agent-accounts"
main = "src/index.ts"
compatibility_date = "2026-06-01"

routes = [{ pattern = "id.reames-agent.io", custom_domain = true }]

[vars]
# Where the web frontend lives — used for email links and post-verify redirects.
APP_ORIGIN = "https://reames-agent.io"
# Browsers that may call this API with credentials (comma-separated).
ALLOWED_ORIGINS = "https://reames-agent.io,https://www.reames-agent.io"
# Cookie scope. ".reames-agent.io" lets the apex + any subdomain (and the future CLI)
# share the session. Leave empty for host-only (local dev).
COOKIE_DOMAIN = ".reames-agent.io"
# "stub" logs email links to the worker console (local dev); "resend" sends real
# mail via RESEND_API_KEY.
EMAIL_PROVIDER = "resend"
# From-domain must match a verified Resend domain — send.reames-agent.io is verified
# (DKIM), the apex is not, and Resend rejects sends from an unverified domain.
MAIL_FROM = "Reames Agent <no-reply@send.reames-agent.io>"
# Comma-separated emails minted as admins on first sign-up.
ADMIN_EMAILS = ""

[[d1_databases]]
binding = "DB"
database_name = "reames-agent-accounts"
database_id = "bae20011-a6fe-4fad-ae61-a3aaac605e0c"
migrations_dir = "migrations"

# Per-IP throttle for the sensitive auth endpoints (login/register/forgot/reset).
[[unsafe.bindings]]
name = "AUTH_LIMITER"
type = "ratelimit"
namespace_id = "2001"
simple = { limit = 10, period = 60 }
