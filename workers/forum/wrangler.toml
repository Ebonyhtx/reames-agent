# Reames Agent Community forum API. Identity from id.reames-agent.io; content + anti-abuse
# state in its own D1. First deploy:
#   wrangler d1 create reames-agent-forum        # paste database_id below
#   wrangler d1 execute reames-agent-forum --remote --file=schema.sql
#   wrangler d1 execute reames-agent-forum --remote --file=seed.sql
#   wrangler deploy
name = "reames-agent-forum"
main = "src/index.ts"
compatibility_date = "2026-06-01"

routes = [{ pattern = "forum.reames-agent.io", custom_domain = true }]

[vars]
# Where the forum web app lives (for CORS + login redirects).
APP_ORIGIN = "https://reames-agent.io"
ALLOWED_ORIGINS = "https://reames-agent.io,https://www.reames-agent.io"
# Shared identity service that resolves the session.
ID_ORIGIN = "https://id.reames-agent.io"
# Emails minted as forum admins/moderators on first sign-in.
ADMIN_EMAILS = "359807859@qq.com"

[[d1_databases]]
binding = "DB"
database_name = "reames-agent-forum"
database_id = "294f6daa-6ee5-4c2d-a98f-54b38a1b3ac0"

# Per-IP throttle for content creation (topics/replies/flags).
[[unsafe.bindings]]
name = "POST_LIMITER"
type = "ratelimit"
namespace_id = "3001"
simple = { limit = 10, period = 60 }
