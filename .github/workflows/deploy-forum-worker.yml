name: Deploy forum worker

on:
  push:
    branches: [main-v2]
    paths:
      - 'workers/forum/**'
      - '.github/workflows/deploy-forum-worker.yml'

# Inherited worker deployment is intentionally not manually runnable from this
# repository. Re-enable only after docs/RELEASING.md production gates pass.

permissions:
  contents: read

concurrency:
  group: deploy-forum-worker
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 10
          run_install: false
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: workers/forum/pnpm-lock.yaml
      - name: Deploy
        working-directory: workers/forum
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          pnpm install --frozen-lockfile
          npx wrangler deploy
