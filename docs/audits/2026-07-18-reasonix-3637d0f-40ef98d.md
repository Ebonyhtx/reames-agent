# 2026-07-18 Reasonix `3637d0f0..40ef98de` 增量收口审计

> 主上游：`esengine/DeepSeek-Reasonix` `main-v2`
>
> 基线：`3637d0f028bb8223d50ba9490a0ab5140eada4f3`
>
> 目标：`40ef98de92a30a273ee582ec682ab338483109d2`

## 结论

本区间共 5 个非 merge 提交、49 个变化文件、`+1658/-410`。机器账本位于
[`reasonix-generation-3637d0f-40ef98d.json`](../upstreams/reviews/reasonix-generation-3637d0f-40ef98d.json)。
本轮没有 provider、Agent loop、permission、sandbox、session persistence 或 Desktop runtime
变化；主要内容是 Reasonix 品牌站点、正式发布链和 CLI 终端体验。

唯一适合 Reames 的代码缺口来自 `40ef98de`，已经按 Reames 现有 Bubble Tea 状态机吸收：

- 鼠标接管开启时，右键在没有 transcript selection 的情况下读取文本剪贴板，并重新进入统一
  `tea.PasteMsg` 路径，继续复用文件引用、长文本折叠、completion 和 textarea repaint；
- SSH 会话不读取远端主机剪贴板冒充本地终端剪贴板，保留 composer 并显示三语提示；
- assistant markdown 使用 Reames 身份和两格 gutter，与 reasoning、tool card 和 turn receipt
  形成稳定层级；恢复会话重放使用同一投影；
- reasoning → answer 与 answer → receipt 各保留一个语义空行，直接回答不增加首行空白。

Hermes 同日 BOM 修复同时暴露 Reames `cron.json` 的 Windows 手工编辑兼容缺口，本批一并采用，
但它不属于 Reasonix 增量。

## 逐提交决策

| 提交 | 内容 | 决策 |
|---|---|---|
| `8a36da8d` | 官网暗色模式、cache-block motif、changelog 页面 | 拒绝；Reames 主仓库没有同构生产品牌站点，P5 主题包只接受原创/可审计资产 |
| `315f6f30` | v1.17.15 release notes | 记录版本信号；没有代码采用项 |
| `e7a0f116` | 防止 stable publisher 被跳过，强化正式发布授权链 | 不复制；Reames production release 仍关闭，候选构建、CodeQL 和发布写权限棘轮继续生效 |
| `840cb6ca` | registry publish button 站点布局 | 拒绝；Reames 没有生产 registry 网站或发布按钮 |
| `40ef98de` | 右键文本粘贴、composer drag copy、footer、assistant transcript hierarchy | 部分采用；文本粘贴/SSH 边界/assistant hierarchy 已实现。composer app-owned selection 依赖 Reasonix 已有 selection model，Reames 当前 textarea 无同构状态，延后；footer 架构不同，Reames data row 始终承载 model/cache/context 且已有精确高度合同，不复制 |

## 明确保持的边界

- 不复制 Reasonix 品牌、站点、图片、registry endpoint、release authorization 或稳定 publisher。
- 不因 UI 改动改变 system prompt、tool schema 顺序或 Controller 传输边界。
- 右键粘贴只在 composer 可见时生效；活动 transcript selection 仍优先复制。
- SSH 只提示使用终端粘贴快捷键或 `/mouse`，不声称远端进程可访问本机剪贴板。
- 本批没有证据要求重开 Memory v5、遥测、crash worker、第二 runtime 或生产发布。

## 验证

```powershell
go test ./internal/cron ./internal/cli -count=1
python -m unittest scripts.test_audit_reasonix_generation -v
python -m unittest scripts.test_check_upstreams -v
```

最终冻结提交前已补跑并通过：Root `build/vet/internal` 全量、Desktop 独立模块
`build/vet/test ./...`、Frontend `test:all`/production build/bundle budget、CLI/cron race、143 项
Python 合同（2 项平台跳过）、Node upstream reconciliation、tool 文档合同、docs/public/deploy/release、
credential-free Gateway baseline smoke、actionlint v1.7.7、Git Bash shell syntax，以及 linux/darwin/windows ×
amd64/arm64 的 CLI 与 Guard 共 12 个 `CGO_ENABLED=0` 构建。远端 CI/CodeQL 仍以最终 push 后的 run 为准。
冻结提交对象还通过 `git clone --no-local` 的 Root/Desktop 全量、空 `node_modules` Frontend
install/test/build/bundle budget，以及 Reasonix/upstream/docs/public 合同复验。
